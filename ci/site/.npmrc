; No lockfile on purpose: the hexo-next-publist file: dependency records the
; plugin version in a lockfile, which would go stale on every release bump.
; Both builds of a render-check run share one install, so the comparison
; itself stays consistent.
package-lock=false
